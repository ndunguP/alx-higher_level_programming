#!/usr/bin/node
// Script that prints 3 lines by using an array of string and a loop
let linesToPrint = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let i = 0; i < linesToPrint.length; i++) {
  console.log(linesToPrint[i]);
}
